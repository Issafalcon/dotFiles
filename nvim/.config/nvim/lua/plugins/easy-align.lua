require('vimp')

-- Start interactive EasyAlign for a motion/text object (e.g. gaip)
vimp.nmap({'silent'}, 'ga', '<Plug>(EasyAlign)')
-- Start interactiv EasyAlign in visual mode (e.g. vipga)
vimp.xmap({'silent'}, 'ga', '<Plug>(EasyAlign)')
